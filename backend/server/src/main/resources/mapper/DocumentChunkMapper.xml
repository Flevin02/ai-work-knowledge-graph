<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flevin.knowgraph.server.repository.mapper.DocumentChunkMapper">

    <!-- 并发重复分片命中业务唯一键时保留先写入的 Snowflake 标识。 -->
    <insert id="insertIfAbsent">
        INSERT INTO document_chunks (
            id, space_id, source_document_id, section_record_id, section_id,
            chunk_id, parser_version, section_path, ordinal, document_ordinal,
            content_text, start_offset, end_offset, content_hash, chunk_version,
            created_at, updated_at
        ) VALUES (
            #{entity.id}, #{entity.spaceId}, #{entity.sourceDocumentId}, #{entity.sectionRecordId},
            #{entity.sectionId}, #{entity.chunkId}, #{entity.parserVersion}, #{entity.sectionPath},
            #{entity.ordinal}, #{entity.documentOrdinal}, #{entity.contentText}, #{entity.startOffset},
            #{entity.endOffset}, #{entity.contentHash}, #{entity.chunkVersion},
            #{entity.createdAt}, #{entity.updatedAt}
        )
        ON DUPLICATE KEY UPDATE id = id
    </insert>

</mapper>
